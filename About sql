#What is database?
  Database refers to collections of data or the way to accessing and manipulating the data
  A structured set of computerized data with an accessible interface
  
#MySQL vs SQL

  SQL is the language we use to talk to our databases and it is stands for structured query language
  
  MySQL is the database management system that implements SQL
  
          Mysql          }
          SQlite         }
          PostgreSQL     } All uses SQL
          ORACLE         }
          Many others    }

  MySQL : SELECT * Users WHERE Age >= 18;

  PostgreSQL : SELECT * Users WHERE Age >= 18; "Same"   (Slight syntax different)

